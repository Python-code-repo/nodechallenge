block head
    link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css')
    link#bootstrap-css(href='//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css' rel='stylesheet')
    script(src='//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js')
    script(src='//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/bootstrap-notify/0.2.0/js/bootstrap-notify.min.js')

    style.
        body {
            margin: 0;
            padding: 0;
            background-color: #17a2b8;
            height: 100vh;
        }
        #login .container #login-row #login-column #login-box {
            margin-top: 120px;
            max-width: 600px;
            height: 320px;
            //- border: 1px solid #9C9C9C;
            //- background-color: #EAEAEA;
        }
        #login .container #login-row #login-column #login-box #login-form {
            padding: 20px;
        }
        #login .container #login-row #login-column #login-box #login-form #register-link {
            margin-top: -85px;
        }
//- (style="border: 1px solid #9C9C9C;background-color: #EAEAEA;")
block content
    html
        head
        body
            #login
                h3.text-center.text-white.pt-5 Appointment Details
                .container
                    #login-row.row.justify-content-center.align-items-center
                        #login-column.col-md-6
                            #login-box.col-md-12
                                .row
                                    .col-md-12
                                        button.btn.btn-primary.mb-1.float-right(type='button' onclick="location.href = '/user/Appointments/"+mydetails._id+"';") New Appointment

                                
                                .row
                                    table.table.table-dark
                                        thead
                                            tr
                                                th(scope='col') Date
                                                th(scope='col') From Time
                                                th(scope='col') To Time
                                                th(scope='col') Doc. Name
                                                th(scope='col') Status
                                        tbody
                                            each data in data1
                                                tr(id="row"+data._id)
                                                    th(scope='row') #{data.appointdate}
                                                    td #{data.fromTime}
                                                    td #{data.toTime}
                                                    td #{data.docId.name}
                                                    td #{data.isaccepted}


block pagescripts
    script.
        function blockUSer(isAcceptted,userId){
            $.ajax({
                    url: "/user/isAcceptted/"+userId+"/"+isAcceptted,
                    success: function (result) {
            
                    },
                    error: function (err) {
                        console.log(err)
                    }
                })
        }